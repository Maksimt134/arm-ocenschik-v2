# ── FastAPI + ASGI-сервер ──────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0     # [standard] тянет uvloop, httptools, websockets
h11>=0.14.0                   # HTTP/1.1 парсер (требуется uvicorn)
anyio>=3.7.0                  # Async I/O абстракция
starlette>=0.27.0             # ASGI-фреймворк под капотом FastAPI

# ── Данные / валидация ─────────────────────────────────────────────
pydantic>=2.5.0
python-multipart>=0.0.9       # Для загрузки файлов через FastAPI

# ── Генерация Word-отчётов ─────────────────────────────────────────
python-docx>=1.1.0
lxml>=4.9.0                   # XML-движок для python-docx

# ── Десктопное окно ────────────────────────────────────────────────
pywebview>=4.4.0              # Нативное окно на Windows (EdgeChromium/MSHTML)

# ── Сборка портативного .exe ───────────────────────────────────────
pyinstaller>=6.3.0
